name: Autograding Tests
<<<<<<< HEAD

on:
  push:
  workflow_dispatch:

=======
on:
- push
- workflow_dispatch
>>>>>>> 5ce5120 (Replace workflow with classroom-provided autograding configuration)
permissions:
  checks: write
  actions: read
  contents: read

jobs:
  run-autograding-tests:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'   # change if your classroom requires a specific version

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pytest

      - name: Run pytest
        run: |
          python -m pytest -q

      # Keep this if your classroom uses autograding.json (GitHub Classroom)
      - name: GitHub Classroom Autograding
        uses: education/autograding@v1
=======
    - name: Checkout code
      uses: actions/checkout@v4
    - name: tests
      id: tests
      uses: classroom-resources/autograding-python-grader@v1
      with:
        timeout: 10
        setup-command: |
          pip install -r requirements.txt
          export PYTHONPATH=.
    - name: Check test results
      id: check_results
      run: |
        if [ -z "${{steps.tests.outputs.result}}" ]; then
          echo "::error::No test results found. Tests may have failed to run."
          echo "result=error" >> $GITHUB_OUTPUT
        else
          echo "result=${{steps.tests.outputs.result}}" >> $GITHUB_OUTPUT
        fi
    - name: Autograding Reporter
      if: steps.check_results.outputs.result != 'error'
      uses: classroom-resources/autograding-grading-reporter@v1
      env:
        TESTS_RESULTS: "${{steps.tests.outputs.result}}"
      with:
        runners: tests
>>>>>>> 5ce5120 (Replace workflow with classroom-provided autograding configuration)
